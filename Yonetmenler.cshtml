@model IEnumerable<film.Models.YonetmenViewModel>

@{
    ViewData["Title"] = "Yönetmenler";
}

<h1>Yönetmenler</h1>

<div class="row">
    @foreach (var yonetmen in Model)
    {
        <div class="col-md-6 mb-4">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">@yonetmen.YonetmenAdi</h5>
                </div>
                <div class="card-body">
                    <div class="list-group">
                        @foreach (var film in yonetmen.Filmler)
                        {
                            <div class="list-group-item">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="mb-1">@film.FilmAdi</h6>
                                        <small class="text-muted">
                                            Tür: @film.FilmTuru | 
                                            IMDB: @film.IMDBPuani | 
                                            @film.CikisYili
                                        </small>
                                    </div>
                                    <div>
                                        @if (film.Izlendimi)
                                        {
                                            <span class="badge bg-success">İzlendi</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">İzlenmedi</span>
                                        }
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
                <div class="card-footer text-muted">
                    Toplam: @yonetmen.Filmler.Count film
                </div>
            </div>
        </div>
    }
</div>

<div class="mt-3">
    <a asp-action="Index" class="btn btn-secondary">Film Listesine Dön</a>
</div> 